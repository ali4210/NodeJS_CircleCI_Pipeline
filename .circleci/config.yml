version: 2.1

jobs:
  build-and-test:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      
      # ======================================================
      # THIS IS THE MISSING STEP THAT FIXES YOUR ERROR
      # ======================================================
      - run:
          name: Install Dependencies
          command: npm install
      # ======================================================

      - run:
          name: Run Tests
          command: npm run test

workflows:
  version: 2
  sample-workflow:
    jobs:
      - build-and-test